@model Propietario

<h1>Eliminar Propietario</h1>

<p>¿Confirmás que querés eliminar a <strong>@Model.Nombre @Model.Apellido</strong>?</p>

<dl class="row">
    <dt class="col-sm-2">DNI</dt>
    <dd class="col-sm-10">@Model.Dni</dd>
    <dt class="col-sm-2">Email</dt>
    <dd class="col-sm-10">@Model.Email</dd>
</dl>

<form asp-action="Delete" asp-route-id="@Model.IdPropietario" method="post">
    <button type="submit" class="btn btn-danger">Eliminar</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>